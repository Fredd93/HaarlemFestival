// danceLogic.js
document.addEventListener('DOMContentLoaded', () => {
    // fetchDanceDays() or do something to build the day buttons
});

// Example event handler if you create day buttons in JS:
function handleDanceTabClick(day) {
    // fetch the events for "day" from your API, fill the table
}
